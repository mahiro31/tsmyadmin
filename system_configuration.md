# システム構成図

## フロントエンド
- **フレームワーク**: Angular (TypeScript)
- **役割**: データベース管理用UIを提供する

## バックエンド
- **サーバー**: Node.js + Express (TypeScript)
- **役割**: APIを通じてデータベース操作を処理する

## データベース接続
- **ライブラリ**: `mysql2`
- **役割**: MySQLへの接続と操作

## 認証とセキュリティ
- **認証方法**: JWT
- **役割**: セキュアなデータベース操作を保証するための権限管理

## 環境設定とデプロイメント
- **ツール**: Docker
- **役割**: 開発環境と本番環境での一貫性を確保する

## Docker開発環境
- **Dockerfile**:
  - ベースイメージ: `node:16-alpine`
  - アプリケーションディレクトリ: `/app`
  - 必要なファイルのコピーと依存関係のインストール
  - ホストマシン上のファイルをコンテナ内にマウントし、リアルタイムで変更を反映させます。
- **Docker Compose**:
  - サービス定義: Node.jsサーバーとMySQLデータベース
  - ボリュームマウント: アプリケーションファイルの変更を反映
  - 環境変数: 設定ファイルや接続文字列

## 必要なモジュール
- **Docker Compose**: `docker-compose` をインストールして、複数のコンテナを管理します。
- **MySQLクライアント**: データベース操作やデバッグに使用します。

## 変更点
- Docker開発環境のセットアップ情報を追加しました。
- 必要なモジュールとしてDocker ComposeとMySQLクライアントを追記しました。
- フロントエンドのフレームワークを Angular (TypeScript) に変更しました。
- JWT を導入し、セキュリティ強化を図りました。
